<Page x:Class="VirtualClassroom.StudentClient.ViewMarksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="ViewMarksPage">

    <Grid>
        <GroupBox Header="Marks">
            <DataGrid x:Name="dataGridMarks" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Path=Date}" />
                    <DataGridTextColumn Header="Subject Name" Binding="{Binding Path=SubjectName}" />
                    <DataGridTextColumn Header="Lesson Name" Binding="{Binding Path=LessonName}" />  
                    <DataGridTextColumn Header="Value" Binding="{Binding Path=Value}" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Page>

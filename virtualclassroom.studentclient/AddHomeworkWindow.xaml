<Window x:Class="VirtualClassroom.StudentClient.AddHomeworkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Homework" SizeToContent="WidthAndHeight"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid>
        <StackPanel>
            <WrapPanel Margin="10">
                <Label>Homework File:</Label>
                <TextBox x:Name="txtHomeworkPath" Width="150" Margin="10 0 0 0"/>
                <Button x:Name="btnBrowseHomework" Click="btnBrowseHomework_Click" Margin="10 0 0 0">Browse</Button>
                <Button x:Name="btnOpenEditorHomework" Click="btnOpenEditorHomework_Click" Margin="10 0 0 0">Open Editor</Button>
            </WrapPanel>
            <WrapPanel HorizontalAlignment="Right">
                <Button x:Name="btnAdd" Click="btnAdd_Click" Margin="10" Padding="5" >Add</Button>
                <Button x:Name="btnCancel" Click="btnCancel_Click" Margin="10" Padding="5">Cancel</Button>
            </WrapPanel>
        </StackPanel>
    </Grid>
</Window>
